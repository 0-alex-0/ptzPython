this is a simple python v4l2-ctl gui control
you will need:
  tkinter for python
  v4l2-ctl to interface with your PTZ USB Camera

run the install.sh script from same directory to install
if you need to delete, created directory is /usr/share/ptzPython
